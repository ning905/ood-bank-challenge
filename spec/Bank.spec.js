const Bank = require("../src/Bank");

describe("Bank", () => {
  let bank;

  beforeEach(() => {
    bank = new Bank();
  });

  it("", () => {
    //set up
    //execute
    //verify
    expect(expected).toEqual(result);
  });

  it("", () => {
    //set up
    //execute
    //verify
    expect(expected).toEqual(result);
  });

  it("", () => {
    //set up
    //execute
    //verify
    expect(expected).toEqual(result);
  });

  it("", () => {
    //set up
    //execute
    //verify
    expect(expected).toEqual(result);
  });

  it("", () => {
    //set up
    //execute
    //verify
    expect(expected).toEqual(result);
  });
});
